# Private Configs

Copy the templates from `configs/templates/` into this folder and edit the
paths (dataset, model checkpoints, prompt files) for your environment.  These
files are ignored by git to prevent accidental leakage of private paths or API
keys.
